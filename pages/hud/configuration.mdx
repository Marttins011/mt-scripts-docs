## Main config
config.lua is the Configuration file of the script

```lua copy filename="mt-hud/config.lua"
Config = Config or {}

Config.Core = 'qb-core'

Config.speedType = 'kmh' -- kmh or mph

Config.updateDelay = 300 -- in millisecodns
```